<%# 編集画面。フォームは一覧の追加フォームと共通 (_form.html.erb)。 %>
<% content_for :title, t(".title") %>

<h1 class="mb-6 text-2xl font-bold"><%= t(".title") %></h1>

<%= render "form", todo: @todo %>

<p class="mt-6"><%= link_to t(".back"), root_path, class: "text-sm text-indigo-600 hover:underline dark:text-indigo-400" %></p>
